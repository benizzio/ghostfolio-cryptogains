# Changelog

## 0.1.0

- Add the initial reusable Spec Kit extension
- Generate `data-model-diagram.mmd` from `data-model.md` via agent inference
- Register a mandatory `after_plan` hook for automatic generation
